custom UDP rule
port 1337

sudo apt-get update
sudo apt-get install gcc
sudo apt-get install make

connect
from W:\
ssh -i "ehsa-svn.pem" ubuntu@ec2-3-25-105-63.ap-southeast-2.compute.amazonaws.com


download
wget http://enet.bespin.org/download/enet-1.3.15.tar.gz

extract
tar xvzf enet-1.3.15.tar.gz

build lib
sudo ./configure && sudo make && sudo make install

build.sh
#!/bin/bash

export LD_LIBRARY_PATH=/usr/local/lib

gcc chat_server.c -o chat.srv -Iexternal/include -lenet // don't need the includes?